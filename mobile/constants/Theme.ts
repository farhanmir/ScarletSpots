/**
 * Lunar Glass Design System
 *
 * Tokens live in `constants/theme.ts` alongside Colors and Fonts.
 * Import via:  import { Theme } from '@/constants/theme';
 *
 * This file is excluded from TypeScript compilation (see tsconfig.json) to
 * avoid a case-sensitivity conflict with theme.ts on case-insensitive OSes.
 */
